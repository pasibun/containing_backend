package org.nhl.containing_backend.vehicles;

import org.nhl.containing_backend.models.ContainerHolder;

/**
 * Thing that moves and holds containers.
 */
public abstract class Vehicle extends ContainerHolder {
    public Vehicle() {
        super();
    }
}
